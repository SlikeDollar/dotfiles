--config-locations ~/.config/yt-dlp/config
# Always extract audio
-x

# Save to that directory
-o ~/music/youtube-dl/%(title)s.%(ext)s

# Audio Format
--audio-format "mp3"

# Add artist to metadata(cmus hack)
--parse-metadata "your artist: %(album_artist)s"
--embed-metadata
